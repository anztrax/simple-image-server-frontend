const keyDownHandlers = {
  ArrowDown(event){
    event.preventDefault();
  },
  ArrowUp(event){
    event.preventDefault();
  },
  Enter(event){
    event.preventDefault();
  },
  Escape(event){
    event.preventDefault();
  }
};

export default keyDownHandlers